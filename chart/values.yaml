
tlsSecret:
  secrets:
    - name: wildcard-webui-museai-cc
      key: "" # set in ./secrets/values.yaml
      crt: "" # set in ./secrets/values.yaml

yumBackend:
  imageTag: v0.7.3
  env: [] # set envs in ./secrets/values.yaml

gatewayHost: "" # set in ./secrets/values.yaml
